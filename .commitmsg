v1.2.0: Deep audit fixes, dead code removal, new features & tests

P0 Critical:
- Delete 11 dead HomeAuto screens (contracts/, main/, settings/)
- Consolidate duplicate navigation types into types/navigation.ts
- Remove ghost expo-document-picker plugin from app.json
- Fix Split4UsBalances crash (nonexistent route -> Split4UsExpenses)
- Remove dangerous `as never` navigation casts via proper typing

P1 Bug Fixes:
- Fix password minimum mismatch (Register: 6 -> 8 chars, matching AuthContext)
- Remove hardcoded Supabase URL fallback (security)
- Add error checking to group/expense delete operations
- Fix SplitConfigurator missing initial mount calculation
- Fix SplitConfigurator participant sync when props change

Navigation & Types:
- Extend global RootParamList with Split4UsTabParamList
- Make CreateExpense params fully optional
- Simplify DashboardScreen to use untyped useNavigation()
- Fix all import paths from deleted navigation/types.ts

New Features:
- Add NotificationsScreen route to RootStackNavigator
- Add notification bell button to Dashboard header
- Integrate OnlineStatusIndicator in App.tsx
- Add DateTimePicker to CreateExpenseScreen
- Add CSV export button to GroupDetailScreen
- Add error state to BalancesScreen
- Show user display names instead of "Person X" in splits

Testing (60 tests, 3 suites):
- New: export.test.ts (9 tests for CSV export & balance summary)
- New: api.test.ts (7 tests for API client with mocked supabase/fetch)
- New: formatDate & formatRelativeTime tests in utils.test.ts
- Fix Jest setupFiles (remove broken extend-expect path)

Config:
- Bump version 1.1.0 -> 1.2.0
- Remove unused date-fns dependency
- Clean up Jest configuration
